import { AdminUserPipe } from './admin-user.pipe';

describe('AdminUserPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUserPipe();
    expect(pipe).toBeTruthy();
  });
});
