import { AdminCategoryPipe } from './admin-category.pipe';

describe('AdminCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
